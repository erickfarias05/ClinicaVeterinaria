@model IEnumerable<Modelo.Cadastros.Veterinario>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.cmv)
            </th>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.cmv)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.VeterinarioId }) |
                @Html.ActionLink("Details", "Details", new { id=item.VeterinarioId }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.VeterinarioId })
            </td>
        </tr>
    }
    
    </table>
</body>
</html>
